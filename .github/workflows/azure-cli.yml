name: dev

on: 
  workflow_dispatch:

    
jobs:

  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_AD_SUBS_LEVEL }}
      
      - name: Checkout
        uses: actions/checkout@v1

      - name: Echo env
        run: |
          echo github.ref
          
      
