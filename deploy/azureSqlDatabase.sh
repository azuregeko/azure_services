#az accout set $subscription

#az sql db create -g $resourceGroup -s $sqlServerName -n $dbNameETM --service-objective S0
#az sql db create -g $resourceGroup -s $sqlServerName -n $dbNameEUS --service-objective S0